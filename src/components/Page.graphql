query Page($slug: String!) {
  pageCollection(where: { slug: $slug }) {
    items {
      title
      slug
      contentCollection {
        items {
          componentName
          componentType
        }
      }
    }
  }
}
