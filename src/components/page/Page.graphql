fragment page on Page {
  title
  slug
  contentCollection {
    items {
      componentName
      __typename
    }
  }
}

query Pages {
  pageCollection {
    items {
      ...page
    }
  }
}

query PageItem($slug: String!) {
  pageCollection(where: { slug: $slug }) {
    items {
      ...page
    }
  }
}
